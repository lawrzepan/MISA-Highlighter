message: res string "Hello, World!", 0.0+yellow

def foo 5^5

bmk "start"

_start:
    mov a0, message             # Load the address of the string into a0
    syscall SYS_PRINT_STRING    # Output the string to the terminal
    exit